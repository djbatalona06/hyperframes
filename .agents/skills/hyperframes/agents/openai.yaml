interface:
  display_name: "Hyperframes"
  short_description: "Repo-specific patterns and workflows for hyperframes"
  default_prompt: "Use the hyperframes repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true